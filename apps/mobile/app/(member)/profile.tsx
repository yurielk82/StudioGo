import { Screen, StyledText } from '@/design-system';

/**
 * 프로필 탭 — Phase 7에서 마이페이지/통계 구현
 */
export default function ProfileScreen() {
  return (
    <Screen>
      <StyledText variant="heading-lg">내 정보</StyledText>
    </Screen>
  );
}
