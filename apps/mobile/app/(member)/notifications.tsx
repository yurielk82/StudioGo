import { Screen, StyledText } from '@/design-system';

/**
 * 알림 탭 — Phase 11에서 알림 목록/읽음 처리 구현
 */
export default function NotificationsScreen() {
  return (
    <Screen>
      <StyledText variant="heading-lg">알림</StyledText>
    </Screen>
  );
}
