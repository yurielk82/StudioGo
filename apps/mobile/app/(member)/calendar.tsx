import { Screen, StyledText } from '@/design-system';

/**
 * 캘린더 탭 — Phase 8에서 월/주/일 3종 뷰 구현
 */
export default function CalendarScreen() {
  return (
    <Screen>
      <StyledText variant="heading-lg">캘린더</StyledText>
    </Screen>
  );
}
